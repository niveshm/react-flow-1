frontend:
  type: frontend
  index_file_name: index.html
  url_path: /
  repo_path: frontend
  assets_path: []
  # Content is static, so there are no real install, build and dev steps
  # ":" is a typical "do nothing" command in a shell.
  install: [":"]
  build: [":"]
  dev: [":"]
  
  system:
    dev:
      port: 3000
